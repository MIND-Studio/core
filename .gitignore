node_modules
dist
# Lockfile intentionally not committed: @mind-studio/ui resolves from GitHub
# Packages (a registry peer), and CI installs with --no-package-lock.
package-lock.json
# The packed tarball IS the consumable artifact; keep it in git so fresh
# clones of any consumer prototype install without running build first.
!mind-studio-core-*.tgz
